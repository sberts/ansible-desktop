---
- hosts: localhost
  connection: local
  become: true

  tasks:
  - name: install packages
    package:
      name:
        - htop
        - tmux
        - vim-nox
        - xinit
        - i3
        - powerline
        - https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb

  - name: install chrome
    package:
      name:
        - https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
